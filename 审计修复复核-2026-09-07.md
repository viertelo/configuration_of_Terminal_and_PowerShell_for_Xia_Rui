# 审计修复复核

> 历史记录：本文结论、行号、版本、工作区状态和测试结果仅对应 2026-09-07 所述阶段。后续代码可能已变更，不能将历史问题清单直接视为当前缺陷，也不能将历史通过记录视为当前验证。原文中的本机临时日志未随仓库分发；链接已改为仓库相对路径，标签行号仍保留作历史定位。当前用法见 [README](README.md)、[部署与回退](部署与回退.md)和[开发文档](开发与部署文档.md)。

## 后续修复记录（2026-09-07）

下文保留 `c484a83` 的历史复核证据。用户授权后，已在当前工作区处理五项问题：

1. 手动回退文档改为调用仓库恢复入口，移除按旧文件名复制、直接删除 AutoRun 和无条件报告成功的示例。
2. Scoop 已安装分支也刷新 PATH 并校验命令；shim 或安装损坏时抛出异常。新增缺失命令失败和刷新后可用两个场景。
3. 新安装在快照目录之外保存 `.last-install-context.json`，绑定快照与 MSYS2 实际根目录。默认恢复可使用匹配的本机记录；显式历史/外部/救援快照仍需 `-Msys2InstallPath`，不从清单信任任意路径。已验证独立和总装追加快照、WhatIf、真实隔离恢复与过期上下文清除。
4. vfox 自动生成预算保持 3 秒；手动 Enable-Vfox 默认 15 秒，可通过 `-TimeoutMs` 调整。通过 AST 将实际生成脚本中可静态识别的 vfox 清理、环境更新和退出调用改写为每次 1.5 秒的受管进程，并在最终提示符设置后激活。没有将这些单次进程预算描述为整个 Profile 的硬超时。
5. README 与两份部署文档删除无依据的 258ms/48 倍等承诺，明确历史测量范围，以及单文件替换、救援目录与整组恢复事务的区别。

验证：四套回归测试在 PS7 7.6.5 与 PS5.1 5.1.26100.9168 下共 8 次均通过；5.1 的参数回显测试改用 10 秒预算，独立的 100ms 强制超时测试保留。本机正常用户权限下的独立 PS7 子进程中，真实 Enable-Vfox 用时 12508ms，`Skipped=false`、`ModuleLoaded=true`，随后一次 prompt 调用 165ms；这是一项功能冒烟测试，不是启动性能基准。

本轮未执行真实安装、部署、卸载或恢复；实际 vfox 验证只影响测试子进程环境并使用它自身的用户缓存。日志在 `tests/tmp-final-*.log` 与 `tests/tmp-vfox-smoke-result.json`。代码尚未提交，用户生效中的 Profile 未自动部署。

## 原复核记录

后续真实部署发现并修复了模拟测试未覆盖的注册表问题：PowerShell 注册表提供程序返回的 Get-Item 句柄可能只读。Set-TerminalRegistryState 改为通过 CurrentUser.CreateSubKey / OpenSubKey(path, true) 显式获取可写句柄。新增可选集成测试 `tests/Verify-RegistryState.ps1`，在唯一临时 HKCU 子键中检查 ExpandString 原值、空字符串、替换和删除，并在结束后清理；PS7 与 PS5.1 下均已通过。

复核日期：2026-09-07。原审计基线 `4b308e7`；复核版本 `c484a83`，包含 `fb98ced` 的代码修复和后续文档修改。开始复核时工作区干净。

结论：主要修复方向正确，正式回退入口的数据保护明显改善；但仍有 5 项需要处理，不能宣布原审计全部关闭。此次未修改业务代码、安装脚本或用户终端配置；仅新增本报告和被忽略规则覆盖的本机验证记录。真实安装、卸载、注册表写入没有执行；交互测速及获取工具初始化文本会使用第三方工具自身的用户缓存。

## 尚需处理的问题

### 1. P1：手动紧急回退示例仍会绕过新保护，并误报完成

位置：[部署与回退.md:136](部署与回退.md)、[部署与回退.md:141](部署与回退.md)。

新部署入口生成 `manifest.json`，使用 `PowerShell7_profile.ps1`、`Terminal_stable.json` 等快照文件名；手动示例仍读取旧版 `PowerShell7-profile.before.ps1` 和 `Terminal-stable.before.json`。新快照中没有这些文件，复制会失败。示例没有设置终止错误策略，正常交互会话中仍会继续删除 AutoRun，最后输出“手动回退完成”。

即使用户选的是旧快照，直接 Copy-Item 也没有救援副本和哈希预检，会覆盖后续编辑；直接删除 AutoRun 无法恢复用户原有值。正式恢复脚本已修复这些问题，文档却仍引导用户走旧的不安全路径。此项为源码与文档静态确认，未在真实注册表执行示例。

建议删除或重写该示例，使其遵循共享恢复程序的清单、预检、救援和注册表恢复规则；恢复失败时不得继续清理或报告成功。

### 2. P2：Scoop 显示已安装、但命令缺失时，直接安装入口仍接受成功

位置：[TerminalSetupCommon.ps1:183](scripts/TerminalSetupCommon.ps1)。

`Install-ScoopAppsIfMissing` 在新安装分支检查退出码、刷新 PATH 并检查命令，这是有效修复；但当 `scoop list` 已包含软件，而 `Get-Command` 找不到命令时，仍只打印“应用已安装”并返回。shim 丢失、PATH 未刷新或安装损坏时，PS7、PS5.1、CMD 这些直接调用该函数的入口仍可继续部署配置。

隔离复现：模拟 `scoop list` 含 starship、命令始终不存在，实际函数仍正常返回，`ListedButMissingCommandAccepted=true`。原有安全测试验证的是“scoop install 返回 1”及上层 fallback 函数，未覆盖这一分支。

建议对已有安装分支也刷新 PATH 并验证实际命令；不能恢复就绪状态时中止。字体等没有命令的包需要单独验证。

### 3. P2：自定义 MSYS2 路径备份正确，但默认回退无法使用它

位置：[TerminalState.ps1:156](scripts/TerminalState.ps1)。

安装时已按实际 MSYS2 根目录捕获 `.bashrc` 和四种 ini，这部分正确。但恢复时重新按默认探测结果建立白名单，没有从本次安装的可信上下文恢复自定义根路径。用户按安装末尾提示直接运行 `Restore-All.ps1`，自定义目录的快照会被判定为非法目标，整个预检失败；即使只选择其它组件，也先校验整份清单。

隔离复现：使用临时自定义 MSYS2 根目录创建真实快照；不传路径的 `Restore-All -BackupDirectory … -WhatIf` 报 `Invalid snapshot target: msys2_bashrc`；补上相同的 `-Msys2InstallPath` 后通过。测试未写入任何 MSYS2 配置。

建议让安装记录和恢复入口明确绑定实际根目录，或至少输出包含 `-Msys2InstallPath` 的准确恢复命令并补充文档。不要为了方便直接取消目标白名单。

### 4. P2：vfox 的超时保护未覆盖初始化文本中的同步调用

位置：[Microsoft.PowerShell_profile.ps1:590](Microsoft.PowerShell_profile.ps1)。

`Invoke-ProfileProcess` 能限制 `vfox activate pwsh` 子进程，但随后 `Invoke-Expression $activation` 在当前进程中执行，不受该时间预算约束。本机 vfox 实际生成的初始化文本在 `New-Module` 内同步执行 `vfox env --cleanup`，这一调用仍可能阻塞。模块导入也仍是同步操作，因此不能据此承诺整个 Profile 或整个 Enable-Vfox 调用有硬超时。

隔离验证仅替换“生成初始化文本”的边界，使其返回一个 3.5 秒的等待语句；实际 Enable-Vfox 执行约 3511ms，且 `VFOX_SKIPPED=false`。这验证了执行阶段没有时间预算，不代表真实 cleanup 已复现卡死。

另外，本机正常权限下 full 模式的三次运行中，真实 vfox activate 均触发 3000ms 超时。手动 Enable-Vfox 使用同一预算，仍可能无法完成本机按需初始化。默认关闭 vfox 已解决其默认启动拖慢问题，但“可用的按需激活”和“完整初始化有界”尚需补测。

建议区分自动启动和用户手动启用的预算，检查实际生成代码里的同步初始化工作，并为该阶段设计可控路径；仅限制生成文本的进程不足以覆盖全部初始化。

### 5. P2：性能与原子性文档声明超出当前证据

位置：[开发与部署文档.md:167](开发与部署文档.md)、[README.md:13](README.md)、[开发与部署文档.md:34](开发与部署文档.md)。

文档将默认交互启动写为约 258ms、提速 48 倍，并称为冷启动。本次直接运行项目的 Measure-ProfileStartup.ps1，确认输入输出均未重定向，默认交互模式实际中位数约 3.14 秒，最小模式约 0.42 秒。不同机器和负载会影响数值；现有证据不能支撑普遍的 258ms 默认交互冷启动承诺。测试只统计 Profile 主体，不含进程创建、首次 prompt 渲染和终端绘制，也不是磁盘冷缓存实验。

`Set-TerminalBytes` 的同目录临时文件和 Replace 是有效的单文件替换修复，但不是“跨卷原子写入”，也不是多文件、注册表整体事务。当前恢复循环可能部分完成后失败，以救援目录和状态日志提供人工恢复依据；这与“全终端无损原子回退”的绝对承诺有差别。

建议按测试环境、配置和统计范围报告真实数据，明确单文件替换与整组配置恢复的边界，去除绝对无损和跨卷原子性承诺。

## 原审计 12 项对照

| 原编号 | 原问题 | 复核结果 |
| --- | --- | --- |
| 1 | 回退覆盖后续内容、无救援、错误仍报成功 | 正式新版入口已修复：预检、救援、失败状态及异常传播通过测试；手动文档仍有问题，见问题 1。 |
| 2 | 部署回退选错安装快照、IncludeCmd 不生效 | 已修复：明确读取 .deployment-path，传入组件集合；先安装后部署的真实入口隔离测试通过。 |
| 3 | CMD 当前目录同名程序执行 | 关键路径已修复：系统工具和已解析工具使用绝对路径，Starship Lua 在安装时生成。本机实际生成 Lua 中 prompt 使用绝对 starship.exe 路径。 |
| 4 | 备份遗漏 NuShell / MSYS2 / CMD | 清单已补齐 zoxide.nu、四种 ini 和注册表状态；CMD 有独立卸载快照。自定义 MSYS2 默认恢复仍有问题，见问题 3。 |
| 5 | 安装失败传播、自检退出码遗漏 | 已修复失败退出码及部署自检阻断；Scoop 已安装但不可执行分支仍遗漏，见问题 2。 |
| 6 | vfox 等同步启动阻塞 | 默认按需策略和生成进程超时有效；初始化文本执行仍无完整预算，本机 vfox 显式启用超时，见问题 4。 |
| 7 | 最小模式判断过晚、PS5.1 缺交互判断 | 已修复主要问题：命令发现、模块导入和 UI 初始化在短路之后；PS5.1 复用相同 Profile。基础 PATH/编码和函数定义仍执行，属当前设计。 |
| 8 | 主题 LiteralPath 通配符导致零复制 | 已修复：逐一复制枚举结果，隔离主题复制测试通过。无任何可用主题源时仍不能保证主题库一定非空。 |
| 9 | NuShell 条件块别名作用域 | 已修复：生成顶层 alias，实际 nu 进程确认 4 个别名可见。 |
| 10 | 清单缺少信任边界、执行快照脚本 | 主要问题已修复：名称/目标/注册表白名单、哈希强制检查，旧快照走仓库恢复程序。哈希不证明外部备份来源可信。 |
| 11 | 直接截断写入冒充原子写入 | 单文件写入已修复，锁冲突保留原字节的测试通过；整组事务与断电保证不能据此推导，见问题 5。 |
| 12 | fif 缓冲全部 rg 结果 | 已改为直接管道并拒绝空查询；Find-LargeFiles 只保留 TopN 对象。大仓库交互取消和峰值内存未单独压测。 |

其它原审计事项：Scoop bootstrap 已固定提交及哈希，模块已固定版本、指定官方 PSGallery 并取消持久信任及 SkipPublisherCheck；快照改为 GUID 且不再复用全局备份目录；Documents 已通过系统已知目录解析并有隔离覆盖；日主题采用排序后按日期选取；Starship 缩小 format 并降低超时。这些代码变化与修复方向一致，但没有执行真实联网安装、Windows 8.1 兼容性或每次 prompt 刷新性能验证。

## 验证结果与范围

| 测试 | PowerShell 7.6.5 | Windows PowerShell 5.1.26100.9168 |
| --- | --- | --- |
| Verify-Configuration | 通过 | 通过 |
| Verify-Deployment | 通过 | 通过 |
| Verify-Safety | 通过 | 首次参数回显子进程超出 3000ms；单独重跑通过 |
| Verify-GeneratedConfiguration | 通过 | 通过 |

5.1 首次失败发生在测试自身启动 powershell.exe 回显参数的阶段，重跑通过；不能把首次失败隐去，也不能仅凭这一次超时认定产品逻辑错误。安装与生成测试均使用隔离目录，安装器和注册表边界被替换。新版测试覆盖比原审计时充分，但生成配置测试将 Starship/Zoxide 生成和可信工具解析设为缺失，不能单独证明所有真实插件链路都正常。

交互测速：正常用户权限、PTY、固定 Catppuccin Mocha 主题，每种配置 3 个新 PowerShell 7 进程。沙箱内因插件缓存权限失败的数据已弃用，下面均来自正常权限重跑。

| 模式 | 最小值 | 中位数 | 最大值 | 备注 |
| --- | ---: | ---: | ---: | --- |
| defaults | 3.012s | 3.142s | 4.121s | vfox、图标、横幅、卡片关闭；三次 ErrorCount=0 |
| minimal | 0.385s | 0.417s | 0.566s | 三次 ErrorCount=0 |
| full | 6.644s | 7.582s | 8.183s | 四个开关开启；三次 vfox 均超时降级 |

旧审计默认模式开启的功能更多，不能直接用旧默认 12.56 秒除以新 defaults 值来宣传同配置提速倍数；full 模式也因 vfox 降级跳过而不是完全相同的成功初始化工作量。

本机原始记录：`tests/tmp-recheck-*.log`、`tests/tmp-startup-metrics/*.json`、`tests/tmp-recheck-probes.ps1` 和 `tests/tmp-recheck-probe-results.json`，均由已有忽略规则排除。没有执行真实恢复、卸载、部署或故障断电实验。

建议先处理问题 1 的可执行文档，再补齐问题 2、3 的入口分支，最后收紧问题 4、5 的实现与描述边界。
