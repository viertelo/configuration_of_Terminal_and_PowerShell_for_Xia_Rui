# 安全性、稳定性、性能与启动速度审计

> 历史记录：本文结论、行号、版本、工作区状态和测试结果仅对应 2026-09-07 所述阶段。后续代码可能已变更，不能将历史问题清单直接视为当前缺陷，也不能将历史通过记录视为当前验证。原文中的本机临时日志未随仓库分发；链接已改为仓库相对路径，标签行号仍保留作历史定位。当前用法见 [README](README.md)、[部署与回退](部署与回退.md)和[开发文档](开发与部署文档.md)。

审计日期：2026-09-07。代码基线：`4b308e7`。未修改业务代码、安装脚本或用户终端配置，未执行真实安装、卸载、部署或回退。新增本报告；隔离复现脚本与测量原始数据在本机 `tests/tmp-audit-20260907/`（已被现有忽略规则覆盖）。正常启动测速会使用各插件自己的缓存。

结论：现有实现有值得保留的防护，但还不能支持“全终端无损原子回退”“毫秒级启动”的承诺。应先修复回退数据丢失、回退入口选错快照、CMD 启动的程序查找风险，再处理安装状态和启动阻塞。

P1 表示应优先处理，可能造成数据损失或自动执行不可信代码；P2 表示影响可靠性、功能或性能，需要修复。以下将已复现问题与静态确认的条件性风险分开说明。

## 已执行验证

| 检查 | 结果 |
| --- | --- |
| Verify-Configuration.ps1 / PowerShell 7.6.5 | 通过 |
| Verify-Configuration.ps1 / Windows PowerShell 5.1.26100.9168 | 通过 |
| Verify-Deployment.ps1 / PowerShell 7、5.1 | 均通过 |
| 新版 Restore-All 隔离复现 | 后续编辑被覆盖，新文件内容被删除，救援快照数量为 0 |
| 安装失败注入 | 模拟 Scoop 退出码 1，上层仍返回 true |
| 主题复制复现 | 源目录 1 个主题，目标目录 0 个主题 |
| NuShell 0.108.0 别名作用域复现 | if 块结束后 ll/la/lt/cat 别名数量为 0 |
| CMD 当前目录程序查找复现 | 优先执行测试目录内只打印标记的 fastfetch.cmd；测试文件已移除 |
| 交互式 Profile 测速 | 4 种配置，每种 3 个独立 PowerShell 7 进程 |

代码发现先使用知识图谱；其索引排除了 scripts 目录，且顶层脚本逻辑和生成的 Shell 配置不能依赖调用图完整表达，因此补充了源文件、生成模板和配置检查。没有对第三方二进制做逆向分析或 CVE 全量扫描；没有验证 Windows 8.1 的实际安装兼容性、断电恢复、Windows Terminal 图形界面启动时间。

## 按优先级排列的发现

### 1. P1：新版回退直接覆盖或删除用户后续内容，没有救援快照

位置：[Restore-All.ps1:78](Restore-All.ps1)，实际写入在 100–114 行。

`manifest.json` 分支逐文件恢复；安装前不存在的文件直接删除，存在的文件直接覆盖。它没有旧版管理器的 `pre-rollback` 救援快照，也不核对当前内容是否还是安装时写入的内容。用户安装后编辑 Profile、Starship 或 NuShell 配置，再回退，会失去这些修改。

隔离复现结果：`OriginalOverwroteEdit=true`、`NewContentDeleted=true`、`RescueCount=0`。测试只操作临时目录内的两个文本文件，没有注册表项目。

此外，缺失或校验失败的备份仅告警并继续，最后仍打印“所有终端配置已成功恢复”。因此可能出现部分回退却报告成功。

建议：先验证全部清单和快照，再捕获当前所有目标的救援副本，最后执行恢复；任何缺失、哈希失败或写入失败应返回失败状态。新文件也应先保存再清理。

### 2. P1：部署回退入口可能选择更早的安装快照

位置：[Restore-TerminalConfiguration.ps1:18](Restore-TerminalConfiguration.ps1)、[Restore-All.ps1:19](Restore-All.ps1)。

部署入口更新 `.deployment-path`，但回退委托优先直接调用 `Restore-All`，后者优先读取 `.last-install-backup`；只有没有安装备份时才考虑部署快照。先安装、后部署、再按部署脚本输出的命令回退，会恢复到安装之前，而不是此次部署之前，并可能影响额外终端。`IncludeCmd` 在常规委托分支也没有传递或应用。

建议：部署和安装分别绑定自己的快照 ID、目标集合和回退入口；委托时明确传入此次部署目录。增加“先安装后部署”的回归场景。

### 3. P1：CMD 自动启动会执行当前目录中的同名工具

位置：[cmd/autorun.cmd:47](cmd/autorun.cmd)，以及开头裸调用的 chcp/doskey/findstr 和 [cmd/clink/starship.lua:7](cmd/clink/starship.lua)。

启动脚本直接调用 `fastfetch`，Lua 中通过命令解释器调用 `starship`，没有固定可信绝对路径。CMD 的程序查找会考虑当前工作目录。若用户在不可信仓库或解压目录打开 CMD，目录内的同名程序可能在用户输入命令之前运行，继承当前 CMD 的权限；管理员终端会放大后果。这需要攻击者能够控制该工作目录，不是远程无条件漏洞。

最小安全复现：测试目录放置只输出 `AUDIT_LOCAL_FASTFETCH_EXECUTED` 的 fastfetch.cmd，运行 `cmd /d /c fastfetch`，输出该标记。验证的是与启动脚本相同的裸命令查找方式，未注册或执行完整 AutoRun。

建议：安装时从可信安装目录确定绝对可执行路径，启动时使用这些路径；系统工具使用 System32 绝对路径。将非交互判断前移到 chcp、doskey 和 Clink 注入之前。

### 4. P1：备份目标没有覆盖安装实际修改的全部内容

位置：[scripts/TerminalSetupCommon.ps1:514](scripts/TerminalSetupCommon.ps1)、[Install-NuShell.ps1:97](Install-NuShell.ps1)、[Install-MSYS2.ps1:86](Install-MSYS2.ps1)。

备份清单包含 NuShell 的 starship.nu，却漏掉同一目录中会被覆盖的 zoxide.nu；MSYS2 的用户级 `MSYS2_PATH_TYPE` 和四种 ini 文件修改也没有对应备份。自定义 `Msys2InstallPath` 没有传给备份函数，可能备份默认目录、修改自定义目录。已有 zoxide 配置被替换后不能通过当前快照找回。

另外，`Deploy-TerminalConfiguration` 的快照只覆盖 PS7 和 Terminal，却还会部署 CMD、覆盖 AutoRun 和 Lua。CMD 的卸载逻辑删除 AutoRun，不恢复安装前的原值。顶层 Install-Cmd 的现代备份能覆盖部分情况，但独立 CMD 配置脚本和部署入口没有等价保护。

建议：先计算本次操作的完整变更清单，并用同一清单驱动备份、应用和恢复；保存注册表值及类型、环境变量存在状态，以及实际选中的 MSYS2 路径。

### 5. P2：安装失败会被报告为成功，部署也忽略自检进程退出码

位置：[scripts/TerminalSetupCommon.ps1:469](scripts/TerminalSetupCommon.ps1)、[Install-NuShell.ps1:37](Install-NuShell.ps1)、[Deploy-TerminalConfiguration.ps1:21](Deploy-TerminalConfiguration.ps1)。

Scoop 辅助函数失败时只告警或返回，上层调用后无条件 `return $true`；NuShell/MSYS2 安装调用还主动丢弃布尔结果。安装器随后继续写入配置并输出完成。隔离替换包管理器边界、令 Scoop 返回 1 后，真实上层函数仍返回 true，未进行网络访问。

部署入口运行独立 pwsh 自检，却不检查 `$LASTEXITCODE`；外部进程非零退出并不自动成为所有 PowerShell 版本下的终止异常。

建议：统一返回结构化结果，校验退出码并在刷新 PATH 后验证实际工具；必需依赖失败应中止后续配置写入。独立验证进程失败必须阻断部署。

### 6. P2：启动同步等待外部工具，vfox 是本机主要瓶颈

位置：[Microsoft.PowerShell_profile.ps1:618](Microsoft.PowerShell_profile.ps1)、629、687、699、703、754 行。

vfox、提示符生成、zoxide、模块导入和 Fastfetch 串行执行，项目层没有超时或取消机制。`try/catch` 可以处理异常，但不能结束一直不返回的子进程。提示卡片还有 VFOX_SKIPPED 超时提示，实际初始化路径却没有设置该状态的超时处理。

本机正常权限、固定主题的三次测量中，vfox 初始化分别耗时 10.66、8.96、9.26 秒。详见下方表格。该结果定位到外部工具调用，不代表已确定 vfox 内部慢的具体原因，也不能推广为所有机器的耗时。

建议：优先将 vfox 改为按需激活；对启动必需的外部调用设置时间预算和可取消的进程管理。模块、横幅按需加载。避免仅提高超时上限来掩盖慢路径。

### 7. P2：最小模式判断太晚，非交互模式仍做多次命令发现

位置：[Microsoft.PowerShell_profile.ps1:58](Microsoft.PowerShell_profile.ps1) 到 [615](Microsoft.PowerShell_profile.ps1)。

`POWERSHELL_PROFILE_MINIMAL` 和重定向判断前，已查询 git/yazi/lazydocker/lazygit/nvim/fd/bat/eza、修改 FZF 环境变量等。即使最小模式也不能跳过这些扫描，本机正常权限测量中位数仍为约 631ms。PATH 长、工具缺失或路径慢时影响会更明显。

Windows PowerShell 5.1 的生成模板没有同等的交互检查，模块和提示符仍会在非交互启动时加载；默认横幅及提示输出可能污染捕获结果。`MINIMAL` 仅限制横幅和卡片。

建议：将最小/非交互判断前移，只保留确实需要的基础设置；便利函数可无条件定义，在用户实际调用时查询工具。PS5.1 使用一致的启动策略。

### 8. P2：本地主题同步实际上不会展开通配符

位置：[scripts/TerminalSetupCommon.ps1:260](scripts/TerminalSetupCommon.ps1)。

`Copy-Item -LiteralPath "$cand\*.omp.*"` 将星号当成文件名字符，不会复制已经枚举到的主题。错误又被 SilentlyContinue 隐藏；全新安装的目标主题目录可能一直为空，导致默认随机主题不工作。已有 122 个主题的当前机器会掩盖这一问题。

隔离复现为源目录 1 个主题、目标目录 0 个。建议直接复制前面 `$found` 对象的 FullName，并在复制后验证数量及可用的保底主题。

### 9. P2：NuShell 的条件块别名不会留在外层作用域

位置：[Install-NuShell.ps1:167](Install-NuShell.ps1)。

ll/la/lt/cat 定义在 if 块中，离开块后不可见。用本机 NuShell 0.108.0 和安装模板同样的片段运行后，`scope aliases` 中这些名称数量为 0，而提示卡片仍称别名已启用。

建议：在顶层生成别名，或用顶层 def 在执行时检查工具。测试生成的 config.nu 的实际命令可见性，不能只检查生成器的 PowerShell 语法。

### 10. P2：新版回退缺少清单信任边界，旧版还执行快照内脚本

位置：[Restore-All.ps1:75](Restore-All.ps1)、[154](Restore-All.ps1)。

新版直接使用清单提供的 Target、Name 和注册表 Key/Name，没有旧版管理器的目标白名单、快照文件名约束，SHA256 还是可选字段。若用户恢复从他人处取得或已被篡改的备份，文件写入/删除范围完全由该清单控制。旧版分支优先执行备份目录内的管理脚本，包括调用方传入 WhatIf 时；WhatIf 本身不能让任意脚本成为安全数据。

这是“不可信备份被用户选中执行恢复”的条件性风险，不意味着仅有文件哈希就能鉴别备份来源。建议只执行仓库中可信的恢复程序，对清单做严格 schema、目标路径和文件名校验，强制所有已有快照文件的哈希字段；来自外部的备份应建立独立来源信任机制。

### 11. P2：所谓原子写入实为直接截断写入

位置：[Manage-TerminalConfiguration.ps1:77](Manage-TerminalConfiguration.ps1)。

`WriteAllBytes` 直接覆盖目标文件；异常回写能处理部分进程内失败，但不能防止进程被终止、断电或其他程序读到半写入文件。两文件更新也不是跨文件事务。新版 Restore-All 和安装器的 Copy-Item/WriteAllText 同样没有事务保证。

建议：在目标同卷目录写临时文件、校验并替换目标；跨文件操作另设恢复日志和重入策略。该项为静态确认，没有在用户配置上注入进程终止或断电故障。

### 12. P2：fif 在显示界面之前缓冲全部搜索结果

位置：[Microsoft.PowerShell_profile.ps1:171](Microsoft.PowerShell_profile.ps1)。

先将完整 rg 输出赋给 `$rgOutput`，再传入 fzf。查询为空时匹配几乎所有非空文本行；在大仓库中会等待搜索结束、分配大量 PowerShell 字符串后才显示交互界面。搜索结果规模增长会同时影响响应和内存。

建议：把 rg 流式管道直接接到 fzf，支持取消，并避免默认空查询全量匹配。`Find-LargeFiles` 同样先遍历并完整排序所有文件，TopN 不代表只读取 N 个；大目录使用有界 Top-N 策略。

## 其他需要处理的风险与限制

- **供应链策略偏宽**：[TerminalSetupCommon.ps1:54](scripts/TerminalSetupCommon.ps1) 直接下载并执行浮动 Scoop 安装脚本；模块安装使用 `-SkipPublisherCheck`、不固定版本且没有显式指定仓库，并持久信任 PSGallery。没有证据表明下载源已被攻击，但不利于可复现部署和来源约束。应明确仓库、版本和校验策略；执行策略不是防恶意代码的安全边界。
- **备份生命周期不可靠**：全局 LAST_TERMINAL_BACKUP_DIR 只要目录存在就复用，没有绑定一次安装事务或过期时间；同一会话再次安装可能仍用很早的快照。目录名精度只有秒，Force 或并发调用可能碰撞。建议事务 ID 加 GUID，并避免长生命周期的全局状态。
- **用户目录硬编码**：PS7/5.1 安装、备份和管理器都固定 USERPROFILE/Documents，未解析 OneDrive 或企业重定向后的 Documents。会出现写入成功但实际 Profile 不加载，以及备份错位置。MSYS2 模板还假设用户目录在 C:/Users。
- **日随机语义不符**：每次启动直接 Get-Random，没有按日期缓存；异常抛到 catch 时仅告警，也不一定执行保底主题。应区分“每次随机”和“每日固定随机”，集中实现可靠降级。
- **提示符持续性能**：starship.toml 把 command_timeout 配到 1000ms，启用 Git 状态、内存和多个运行时模块，未限制总 format。特定仓库中的每次提示符刷新仍可能慢；本次没有把 Profile 初始化耗时当作提示符刷新耗时。

## 启动测量

环境：Windows 11、本机 PowerShell 7.6.5；PTY 输入/输出均未重定向，TERM=xterm-256color。每种配置使用 3 个新进程，固定 Oh My Posh 主题以避免随机主题引入不可比变量。

测量对仓库 Profile 的顶层语句插入 Stopwatch，保留同一作用域执行。下表是 Profile 主体加载时间，包含测量开销；不包含 PowerShell 进程创建、Windows Terminal 绘制、首次实际 prompt 渲染或重启后的磁盘冷缓存。不是严格冷启动基准。沙箱内因插件缓存写入失败得到的数据已弃用，下表来自获准的正常用户权限重跑。

| 配置 | 最小值 | 中位数 | 最大值 |
| --- | ---: | ---: | ---: |
| 固定主题，默认功能全部开启 | 12.39 s | 12.56 s | 14.49 s |
| 关闭横幅、提示卡片 | 11.38 s | 11.79 s | 11.80 s |
| 再关闭 vfox、Terminal-Icons | 1.81 s | 1.87 s | 1.96 s |
| MINIMAL=1 | 0.62 s | 0.63 s | 0.64 s |

默认功能的主要块耗时：vfox 8.96–10.66s，提示符初始化 0.60–0.86s，PSReadLine/PSFzf 0.68–0.76s，Terminal-Icons 0.64–0.67s，横幅及卡片 0.68–0.84s。这些是本机测量，不应推断所有机器都会相同。

优先优化 vfox 的同步初始化，比仅关闭横幅更有收益。安装器生成的 Profile 头部会重新赋值部分环境变量，因此从父进程设开关不一定能覆盖已经安装的文件；实际调整时应统一配置优先级。

## 测试覆盖缺口与修复顺序

现有部署测试硬编码历史 deployment-20260907-060507 快照，覆盖的是旧管理器的双文件回退，不是新版全终端回退。Profile 测试使用最小模式，无法发现实际启动时的插件等待或 NuShell/CMD 的生成结果问题。因此“四次测试通过”不能推出这些路径没有问题。

建议顺序：

1. 统一回退事务、救援备份、目标清单和快照选择，首先保护用户数据。
2. 固定 CMD 启动工具的可信路径，约束恢复清单和快照内可执行内容。
3. 修正安装失败传播、自检退出码、主题复制和 NuShell 别名。
4. 将 vfox 改为按需初始化，前移最小模式判断，限制同步初始化时间。
5. 补测新版回退、失败注入、真实生成配置、路径重定向和启动耗时上限。

已确认的积极措施：PS7 主 Profile 没有直接下载/安装逻辑；对非交互场景已有基础判断；多处使用 LiteralPath；旧管理器包含白名单、哈希预检、并发改动检查和救援快照；已有跨 PS7/5.1 的隔离测试。这些措施可以作为统一改造的基础，但新版流程需要获得同等级保护。
