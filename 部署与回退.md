# 安装、部署与回退

本文按当前仓库实现说明操作范围。所有命令均在仓库根目录执行；没有 `pwsh` 时可从 `powershell.exe` 启动。项目入口见 [README](README.md)，实现说明见[开发与部署文档](开发与部署文档.md)。

## 选择入口

| 目的 | 入口 | 快照指针 / 恢复入口 |
| --- | --- | --- |
| 安装工具并生成 Shell 配置 | `Install-All.ps1` 或独立 Shell 安装器 | `.last-install-backup` → `Restore-All.ps1` |
| 同步仓库配置到已有环境 | `Deploy-TerminalConfiguration.ps1` | `.deployment-path` → `Restore-TerminalConfiguration.ps1` |
| 独立部署 / 撤销 CMD 配置 | `cmd/Install-CmdConfiguration.ps1` | `.last-cmd-backup`，通过 `Install-Cmd.ps1 -Uninstall` 撤销配置 |
| 仅安装字体 | `Install-Fonts.ps1` | 不创建上述配置快照 |
| 管理旧格式两文件快照 | `Manage-TerminalConfiguration.ps1` | 显式指定含 `deployment.json` 的目录 |

指针记录本机路径，不应复制其他机器的指针直接恢复。仓库历史中已有的 `.deployment-path` 也不代表你的最新部署；新操作会更新自己的指针。需要指定快照时使用 `-BackupDirectory`。

## 安装与安装选项

```powershell
# 默认安装配置 PS7、PS5.1、CMD。
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Install-All.ps1

# 全部 Shell，给 PS7 选择固定主题，并应用完整 Terminal 模板。
# -NonInteractive 跳过安装器自身选择提示；外部安装仍可能需要交互。
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Install-All.ps1 -All -ThemeMode Fixed -NonInteractive -ApplyWindowsTerminalSettings
```

`-ThemeMode` 只传递给 PS7。PS5.1 默认 `Random`，固定主题需单独执行 `Install-WinPowerShell51.ps1 -Theme CatppuccinMocha`。总装支持按需跳过 PS7、PS5.1、CMD，以及单独包含 NuShell 或 MSYS2；详细参数见[开发文档](开发与部署文档.md)。

安装可能联网安装包和模块、配置 Scoop bucket、修复 WinGet 源、注册字体、写入 CMD AutoRun、设置 `MSYS2_PATH_TYPE`。`Install-All.ps1` 和 PS5.1 安装器还会写当前用户 `RemoteSigned` 执行策略；总装中的这一步发生在配置快照之前。PS7 安装器会在受限策略下尝试调整当前用户策略。模块安装会解除所选模块文件的下载标记，PS5.1 安装器还会遍历其用户模块目录解除标记。

配置恢复不负责卸载软件、包管理器、模块和字体，也不恢复执行策略、文件下载标记、主题缓存、工具数据或包管理器源。组策略可能覆盖进程和用户执行策略。

`Install-Fonts.ps1` 默认安装到当前用户字体目录；`-AllUsers` 在管理员会话中使用系统字体目录，无管理员权限时会警告并降级为当前用户安装。其独立入口要求本地 `fonts/` 存在且包含 TTF，没有独立的网络下载回退。

## Windows Terminal 的不同写入方式

- PS7 安装器检测到稳定版或预览版配置目录后，按选择调用 `Ensure-WindowsTerminalConfigured`。它以完整模板替换对应设置，并尝试隐藏未安装的 NuShell/MSYS2 入口；不是保留全部用户设置的合并。
- `-NonInteractive` 未配合 `-ApplyWindowsTerminalSettings` 时跳过完整模板应用。直接 PowerShell 调用脚本时可用 `-ApplyWindowsTerminalSettings:$false` 明确关闭。
- NuShell/MSYS2 安装器通过 `Register-TerminalShellProfile` 修改已有设置中的对应入口；这与应用完整模板是两个步骤。
- `Deploy-TerminalConfiguration.ps1` 将仓库 `settings.json` 原样写到稳定版目标，不执行上述动态隐藏逻辑，也不安装 Windows Terminal。

模板含本机壁纸路径和 MSYS2 预设路径。部署前按[美化说明](windows终端美化相关.md)调整；非商店版或便携版 Terminal 的设置位置不在当前目标清册内。

## 配置部署

先确认所需工具已安装，核对仓库模板，再预演：

```powershell
pwsh -NoProfile -File .\Deploy-TerminalConfiguration.ps1 -WhatIf
# 核对后执行：
# pwsh -NoProfile -File .\Deploy-TerminalConfiguration.ps1
# 不部署 CMD 时添加 -SkipCmd。
```

部署先运行 `Verify-Configuration.ps1`，随后在实际执行时创建快照。它向 Documents 下的 PowerShell 目录写入共享 Profile 源文件，向 Windows Terminal 稳定版写入设置，并按选项配置 CMD。Documents 由系统 Known Folder 解析，支持重定向，不直接使用执行部署的宿主 `$PROFILE`。

部署写入 Profile 源文件，不保留安装器曾加在目标文件前的主题默认值头部，也不保留目标文件中的手工编辑。无环境变量覆盖时源 Profile 默认随机主题。

**当前限制：**部署末尾调用 `Ensure-FastfetchConfigured`，但部署快照只选 `PowerShell7`、`Terminal` 和可选 `Cmd`，未选 `Shared`。因此这次 Fastfetch 写入不由部署回退恢复。需要完整保护时，先另存 `~/.config/fastfetch/`，或在独立 PowerShell 进程中创建共享资产快照：

```powershell
pwsh -NoProfile -Command '. ./scripts/TerminalSetupCommon.ps1; Backup-AllTerminalConfigurations -Components Shared -PointerName .last-install-backup'
```

该命令会更新安装快照指针；保留输出目录，以后用 `Restore-All.ps1 -BackupDirectory '实际输出的目录' -Components Shared -WhatIf` 预演恢复。它不会自动将 Shared 合并进部署快照。

`-WhatIf` 不写部署目标或创建部署快照，但部署前验证仍会运行并可能产生、清理测试临时文件。它不是整个工具链的只读审计开关；各 `Install-*.ps1` 不提供通用的 `-WhatIf` 安装预演。

## 恢复安装或部署

```powershell
# 最近一次安装快照：预演后，使用同一命令去掉 -WhatIf 执行。
pwsh -NoProfile -File .\Restore-All.ps1 -WhatIf

# 最近一次部署快照：默认 PS7 + Terminal，-IncludeCmd 才包含 CMD。
pwsh -NoProfile -File .\Restore-TerminalConfiguration.ps1 -IncludeCmd -WhatIf

# 指定快照并只恢复 PS7（将目录替换为本机实际快照目录）。
pwsh -NoProfile -File .\Restore-All.ps1 -BackupDirectory 'D:\backups\chosen-snapshot' -Components PowerShell7 -WhatIf
```

一次新的独立安装会更新 `.last-install-backup`；“最近一次”可能只是某个组件的安装。默认 `All` 只恢复清单已有的目标，不会补出未捕获的组件。`Install-Cmd.ps1 -Uninstall` 撤销 CMD 配置，不卸载 Clink、Starship 等软件。

自定义 MSYS2 根目录记录在 `.last-install-context.json` 中，只有与默认安装指针对应的快照匹配时才使用。显式恢复历史、外部或救援快照时，提供同一根目录；自定义 CMD 目录同理：

```powershell
pwsh -NoProfile -File .\Restore-All.ps1 -BackupDirectory 'D:\backups\chosen-snapshot' -Msys2InstallPath 'D:\Tools\msys64' -WhatIf
# 自定义 CMD 目录还可传入 -CmdTargetDir 'D:\Terminal\cmd'。
```

## 快照保护与失败处理

完整清册为 19 个文件，另有 CMD AutoRun 和 `MSYS2_PATH_TYPE` 两个受管注册表值；实际快照按所选组件裁剪。清单记录原先不存在的文件，以便恢复时清除安装新建的目标。

恢复 `manifest.json` 快照时先验证格式、目标白名单、普通文件与 SHA-256，再创建 `pre-rollback-<guid>` 救援目录。恢复过程中检查文件是否在救援快照后被编辑；已有文件用同目录临时文件替换。结果写入救援目录中的 `restore-status.json`。

这些是单文件保护，不是多文件和注册表的整体事务。中途失败时可能已有部分目标恢复；保留原快照和救援目录，根据错误与状态记录检查，必要时显式恢复救援快照。不要按旧文件名直接覆盖 Profile，也不要通过删除 AutoRun 来代替恢复原值。

旧 `deployment.json` 由 `Restore-All.ps1` 委托给仓库当前的 `Manage-TerminalConfiguration.ps1`。该格式只处理 PS7 Profile 和 Terminal 稳定版两个文件，`-Components`、MSYS2/CMD 目录选项不适用于这一分支。不要执行历史快照中的管理脚本。更多见[备份目录说明](backups/README.md)。

## 操作后确认

新开终端检查提示符、中文、字体、目录跳转和所选 Shell 是否可启动。在交互 PowerShell 中执行 `Test-Environment` 查看依赖状态。静态与隔离测试通过不代表所有软件已在本机安装或所有图形终端行为均已验证。
